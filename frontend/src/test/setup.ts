// Vitest setup file — runs once before any test suite.
// Importing jest-dom extends Vitest's expect with DOM matchers:
// toBeInTheDocument, toHaveValue, toBeDisabled, toHaveClass, etc.
import '@testing-library/jest-dom'
